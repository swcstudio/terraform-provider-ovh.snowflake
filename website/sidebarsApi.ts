import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * API sidebar configuration for OVH Snowflake Terraform Provider
 * Simplified version with only existing documentation
 */
const sidebarsApi: SidebarsConfig = {
  apiSidebar: [
    'intro',
  ],
};

export default sidebarsApi;