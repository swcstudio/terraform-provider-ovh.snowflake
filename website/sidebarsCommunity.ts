import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * Community sidebar configuration for OVH Snowflake Terraform Provider
 * Simplified version with only existing documentation
 */
const sidebarsCommunity: SidebarsConfig = {
  communitySidebar: [
    'contributing',
  ],
};

export default sidebarsCommunity;